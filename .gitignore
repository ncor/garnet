# --- General Operating System Files ---
.DS_Store
Thumbs.db

# --- Logs and Crash Reports ---
# Generally, you don't want to track logs or crash reports
logs/
crash-reports/

# --- Minecraft and Mod Loader Generated Files/Folders ---
# The server/client will generate these on first run or during gameplay
defaultconfigs/ # If you want to distribute these, remove this line. Often ignored if only tracking *config* changes.
config-versions/
saves/
journeymap/
xaero/

# --- Temporary/Session Files ---
options.txt.bak
optionsof.txt # OptiFine/Embeddium/Oculus options
usercache.json
eula.txt
memory_settings.json
packmenu-data.json # Example for a specific mod's data

# --- Mod/Config Synchronization Files ---
# Files used by mods for local tracking/syncing
modpack.toml # Often generated by modpack utilities
config/client-sync/ # Used by some mods to sync client/server config
config/server-sync/
config/ftbquests/sync/ # Example for a specific mod

# --- Installed Mods and Libraries ---
# WARNING: Only ignore this if you are using a tool (like CurseForge/Modrinth launcher)
# to handle mod downloads and only tracking the *manifest* file.
# If you are manually tracking mods, REMOVE this line:
# mods/
# libraries/ # Generally not present in a modpack folder, but good to include if it is

# --- Modpack Creator Tools/Folders (Optional) ---
# Files generated by modpack utilities like MultiMC or Prism Launcher
instance.cfg
mmc-pack.json

.mixin.out
downloads
dynamic-data-pack-cache
dynamic-resource-pack-cache
command_history.txt
fabricloader.log
usercache.json
usernamecache.json